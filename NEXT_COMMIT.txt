fix(network): dynamically detect local machine LAN IP

Resolve an issue where the status bar, launcher badge, and SSH tunnel
helpers displayed a hardcoded placeholder IP (192.168.1.100) instead of
the machine's actual local network IP.

- Add prioritized LAN IP detection in system routes using os.networkInterfaces()
- Implement GET /api/system/network-info endpoint returning primary IP and adapters
- Update ApiClient getLanIp() and add getNetworkInfo() helper
- Update AppContext and StorageService to eliminate hardcoded 192.168.1.100
- Add Local Machine Network & IP configuration section to Settings modal
