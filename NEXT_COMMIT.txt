feat(files): require manual session selection in File Manager

Do not automatically connect to SFTP profiles on File Manager mount
or when adding new split panes; show an interactive session chooser hub.

- Set initial secondary panes and newly added panes to 'unassigned'
- Render interactive Session Selector Hub inside unassigned panes
- Support choosing Local Disk, Active Terminal Tabs, Saved SFTP, or Ad-Hoc
- Conditionally hide path toolbar until a session is selected
