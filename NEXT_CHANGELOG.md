## Bug Fixes
- Fixed an issue where the status bar, connection launcher, and SSH tunnel helpers displayed a placeholder IP (192.168.1.100) instead of your machine's actual local network IP.

## New Features
- Added automatic LAN IP detection with prioritized physical network adapter resolution.
- Added a Local Machine Network & IP configuration section under Preferences to view active LAN IP and optionally configure custom overrides.
